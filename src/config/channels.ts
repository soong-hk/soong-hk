import { VideoItem } from "@/types";

export const WHITELISTED_CHANNELS: string[] = [];
// 暫時清空，使用全域搜尋測試真實結果
// 之後搵到好的中文物理治療頻道 Channel ID 再加返入去

export const MAX_RESULTS_PER_SEARCH = 12;
export const CACHE_TTL_MS = 24 * 60 * 60 * 1000;
